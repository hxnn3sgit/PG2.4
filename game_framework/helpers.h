// helpers.h
// for own STL Datastructures like dynamic arrays and stuff

<template T>
class my_vector {
private:
	T* data;
	int size; // int because of better handle
public:
	my_vector();
	my_vector(int size);
	void append(T x);
	void 
}
